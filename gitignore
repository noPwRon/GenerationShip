# ---- Generation Ship: Python project .gitignore ----
# Place this file at the repository root (same level as Ship/ and README.md).

# --- Python bytecode & caches ---
__pycache__/
*.py[cod]
*$py.class

# --- Virtual environments ---
.venv/
venv/
env/
.conda/
.venv*/

# --- Build / packaging ---
build/
dist/
*.egg-info/
*.egg
.eggs/

# --- Logs & run artifacts ---
*.log
logs/
temp/
tmp/
.cache/
.ipynb_checkpoints/
profile-data/

# --- OS cruft ---
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Editors / IDEs ---
# VS Code: keep the folder ignored but explicitly allow the useful launch & extensions files
.vscode/*
!.vscode/launch.json
!.vscode/extensions.json

# PyCharm / IntelliJ
.idea/
*.iml

# --- Tooling / coverage ---
.coverage
.tox/
.mypy_cache/
.pytest_cache/
coverage.xml
htmlcov/

# --- Documentation builds ---
site/
docs/_build/

# --- Data & simulation outputs (adjust to your workflow) ---
# Keep raw inputs in version control; ignore large or generated outputs.
data/raw/*.tmp
data/processed/
outputs/
reports/
results/
runs/
artifacts/

# --- CAD / drawings / exports (optional; un-ignore specific final assets) ---
schematics/*.svg
schematics/*.pdf
# Example: keep a canonical diagram if needed
#!schematics/master_plan.svg

# --- EnergyPlus / Modelica artifacts (future-proofing) ---
eplusout.*
*.idf.tmp
*.err
*.eso
*.mtr
*.rdd
*.mdd
*.dbg
*.csv  # if generated by sims; comment out if you want to track CSV results

# --- OS / shell histories ---
*.swp
*.swo
*.bak
*.orig

# --- Node/Front-end (if you add a UI later) ---
node_modules/
npm-debug.log*
yarn-error.log
pnpm-debug.log

# --- Keep these tracked even if parent dirs are ignored ---
!.gitkeep
README.md
Ship/**

# ------------------------------------------------------
# Tips:
# - Use .gitkeep files to preserve empty folders you want in the repo.
# - Remove exceptions above if you prefer to ignore certain subtrees.
# - For deterministic environments, commit a minimal requirements.txt.
